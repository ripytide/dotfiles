.shell pwsh

alt + y : start wt

alt + i : komorebic toggle-monocle
alt + n : komorebic cycle-focus next
alt + e : komorebic cycle-focus previous
alt + j : komorebic close
alt + k : komorebic toggle-float
alt + p : komorebic toggle-pause
alt + f : komorebic flip-layout horizontal
alt + v : komorebic flip-layout horizontal

alt + s                 : komorebic focus-workspace 0
alt + h                 : komorebic focus-workspace 1
alt + t                 : komorebic focus-workspace 2
alt + d                 : komorebic focus-workspace 3
alt + r                 : komorebic focus-workspace 4
alt + w                 : komorebic focus-workspace 5
alt + shift + s         : komorebic move-to-workspace 0
alt + shift + h         : komorebic move-to-workspace 1
alt + shift + t         : komorebic move-to-workspace 2
alt + shift + d         : komorebic move-to-workspace 3
alt + shift + r         : komorebic move-to-workspace 4
alt + shift + w         : komorebic move-to-workspace 5

alt + oem_plus          : komorebic resize-axis horizontal increase
alt + oem_minus         : komorebic resize-axis horizontal decrease
alt + shift + oem_plus  : komorebic resize-axis vertical increase
alt + shift + oem_minus : komorebic resize-axis vertical decrease

